# Installation

ENNUF is available to install with `conda` or with `pip`:

```commandline
conda install ennuf
```

or

```commandline
pip install ennuf
```

As ENNUF is available on PyPI, it may also be installed with other tools that access PyPI such as `poetry`

```commandline
poetry add ennuf
```
